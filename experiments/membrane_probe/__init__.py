"""Membrane probe — m=0 vs m>0 on gama's meshflow human governance membrane.

Unlike tehai (where the membrane sits on the *upper improvement loop*), gama's
membrane lives on the *task ship-gate*: MeshflowBackend returns ``<<NEEDS_HUMAN>>``
when an artifact is unresolved AND stakes are high, rather than silently shipping
a failing draft (soshiki-genron PAPER §6.5, organizational form ③).

This experiment measures what that membrane buys — WITHOUT modifying the gama
package. The m=0 variant is just MeshflowBackend with ``stakes_threshold`` set
above any stakes, so the gate never fires and the loop always ships best-effort.
m>0 is the normal threshold. Same backend, same cases; only the membrane differs.

Third governance-membrane data point: tehai (upper loop) + soshiki (theory m*) +
gama (task ship-gate).
"""
